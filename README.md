# CounterStrike2D #

## Instalar SDL2 ##
- sudo apt-cache search libsdl2
- sudo apt-get install libsdl2-dev
- sudo apt-get install libsdl2-image-dev

## Build ##
- mkdir build
- cd build
- cmake ..
- cmake --build .
